model: "random_forest"
random_state: 42

random_forest:
  n_estimators: 100
  max_depth: None
  min_samples_split: 2
  min_samples_leaf: 1
  max_features: "auto"
  bootstrap: True

decision_tree:
  max_depth: None
  min_samples_split: 2
  min_samples_leaf: 1
  max_features: "auto"
