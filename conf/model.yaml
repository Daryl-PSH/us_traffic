model: "random_forest"
random_state: 42

random_forest:
  n_estimators: 2
  max_depth: 2 # Null in yaml = None in python
  min_samples_split: 2
  min_samples_leaf: 1
  max_features: "auto"
  bootstrap: True

decision_tree:
  max_depth: null 
  min_samples_split: 2
  min_samples_leaf: 1
  max_features: "auto"
